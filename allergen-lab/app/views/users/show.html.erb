<h1><%=@user.name %></h1>

<h2>
  <% @user.recipes.each do |recipe| %>
    <%= link_to recipe.name, recipe_path(recipe) %><br>
  <% end %>

</h2>
<%= form_tag "/users/#{@user.id}", method: :delete %>
<%= submit_tag 'Delete'%>
